library(blogbuilder)
library(distill)

blogbuilder::exclude_docs()
blogbuilder::iamstudent()

# pull the repository from git
create_post("Lyamuya_HW1", draft = TRUE)

# Do the homework
# Knit the homework
# Pull from the repository
# Commit
# Then, pull, and finally push.
